---
title: "A second placeholder, so the index has more than one row"
description: "Exists only so the two-column index, the tag filter and the pagination have something to act on. Delete before publishing anything real."
pubDate: 2026-07-14
tag: "Frontend"
placeholder: true
---

Scaffolding. This one carries a different tag from the first so the filter has
two states to switch between, and it is plain Markdown rather than MDX to check
that both extensions render through the same template.
