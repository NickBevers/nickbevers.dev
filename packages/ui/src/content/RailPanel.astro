---
interface Props { class?: string; }
const { class: className } = Astro.props;
---
<div class:list={["panel", className]}><slot /></div>

<style>
  .panel {
    border: var(--border-strong);
    background: var(--surface);
    font-family: var(--font-sans);
  }
</style>
